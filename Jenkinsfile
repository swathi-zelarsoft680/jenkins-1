pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello'
            }
        }
    }
}

post {
    always {
     echo "Post Action"
    }
  }

}